checks for silver.security_events:
  - row_count >= 1
  - missing_count(event_ts) = 0
  - duplicate_count(event_id) = 0
  - failed rows:
      name: severity_allowed
      fail condition: severity not in ('low','medium','high')
